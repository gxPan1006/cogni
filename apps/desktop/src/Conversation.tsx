// Temporary stub — Task 22 replaces this with the real streaming conversation view.
export function Conversation(_: {
  token: string;
  threadId: string;
  onTitleMaybeChanged: () => void;
}) {
  return <div>conversation</div>;
}
