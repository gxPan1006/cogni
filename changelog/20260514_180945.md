# 20260514_180945

## Summary

落地 SP-1「脊梁」实现计划:24 个任务、7 个阶段,从空仓库到一个穿透「账户/数据/runner 抽象/云↔桌面拓扑」四根支柱的最小端到端闭环。

## Changes

### 实现计划
- 新增 `docs/superpowers/plans/2026-05-14-cogni-sp1-spine.md`
  - Phase 0:pnpm monorepo 脚手架(contract/cloud/runner-host/shared)
  - Phase 1:`contract` 包 —— runner 抽象类型 + host/client WS 协议 zod schema + domain 视图类型
  - Phase 2:`cloud` 数据层 —— drizzle schema + Neon client + pglite 测试库 + 四组 repository(users/hosts/threads/sessions,含 per-thread seq)
  - Phase 3:`cloud` 域逻辑 —— HostRouter + ClientHub + ChatDomain 编排器(全部 transport-free 单测)
  - Phase 4:`cloud` 服务端 —— Hono + JWT auth + Google OAuth + host WS + client WS/HTTP + headless 端到端集成测试
  - Phase 5:`runner-host` 独立 daemon —— 配置、Claude Code adapter(stream-json→RunnerEvent)、runner-manager、registry client
  - Phase 6:`apps/desktop` —— Tauri 2 + React 壳:deep-link OAuth、侧边栏、对话流式渲染、daemon 注册与拉起
  - Phase 7:全栈冒烟验证 + README/运行指南
  - 每个任务遵循 TDD 步骤(逻辑任务)或 build+verify(集成任务),含完整代码、精确文件路径、验证命令与预期输出
  - 自查修正:统一 `AnyDb` 类型(Neon client 与 pglite 测试库共用一个签名)、补全 cloud 包 e2e 测试的 `ws` 依赖
