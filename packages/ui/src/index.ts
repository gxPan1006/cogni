// @cogni/ui — shared React UI for cogni desktop + web.
//
// Components and hooks land here as the apps/desktop → apps/web extraction
// progresses (SP-2 Section 8). Until each entry is moved in, it lives in
// apps/desktop/src/ — see SP-2 plan Tasks 21-24 for the migration order.

export {};
