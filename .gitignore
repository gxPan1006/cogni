node_modules/
dist/
# Bun-compiled runner-host single binary (~55MB) — generated, never committed
dist-bin/
build/
*.log
.DS_Store
.env
.env.*
!.env.example
# Vite build-time env (no secrets — just public cloud URL). Tracked so CI / fresh
# checkouts pick up production target without manual setup.
!apps/desktop/.env.production
!apps/web/.env.production
.worktrees/
# Ignore .claude/ local state, but track shared project slash-commands
.claude/*
!.claude/commands/
.superpowers/
*.tsbuildinfo
# Per-commit work logs kept locally only (per ~/.claude/CLAUDE.md convention)
changelog/
# Playwright MCP screenshots / snapshots from local UI verification
.playwright-mcp/
.claude-supervisor/

# Internal engineering notes — kept on the maintainer's machine but not
# checked into the public repo. (Previously tracked; untracked at
# open-sourcing time. Stay out of pull requests.)
MEMORY.md
HANDOFF-NOTES.md
tbd.md
docs/integration-log.md
docs/sp3-design-brief.md
docs/SP-2-E2E-VERIFICATION.md
docs/SP-2-OPS-MANUAL.md
docs/SP1-functional-test-plan.md
docs/DEPLOYMENT.md
docs/deploy/
docs/task-card-style-options.html
.scratch/
LAUNCH.md
